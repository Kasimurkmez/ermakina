import React from 'react';

// This is a placeholder component.
// An empty .tsx file can cause errors, so this provides a valid module.
const ImageDescriber: React.FC = () => {
    return null;
};

export default ImageDescriber;
